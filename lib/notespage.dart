import 'package:flutter/material.dart';

class Notesaa extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          "Not Completed",
          style: TextStyle(fontSize: 30, color: Colors.white),
        ),
      ),
    );
  }
}
